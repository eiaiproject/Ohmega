# Antislop fix report 001, 2026-09-14

Covers audit `anti-slop/audit-001-2026-09-14.md`, findings 1-19. All fixed.

## Changes

- Nav anchor (1): removed duplicate `id="tentang"` from `Footer.astro`; `ProducerSection.astro` keeps the sole `id="tentang"`, so `Tentang -> /#tentang` lands on the producer block. Caveat: E2E `tests/e2e.spec.ts:66-84` expects bare `#beranda`-style hrefs while the header renders `/#tentang`-style hrefs; that pre-existing mismatch was left for the test owner.
- Empty states (2): `ProductsSection.astro` shows "Stok sedang disiapkan" when no products are available; `BlogTeaserSection.astro` shows a "sedang disiapkan" notice and hides "Lihat Semua" when empty; blog index empty message kept. States are code-present and hidden when data exists; no error-state path exists because both sources are build-time static (no fetch to fail).
- Contrast (3, 4): `.btn-whatsapp` text is now `--color-primary-dark` `#173F2C` on `#25D366` (5.94:1) and on hover `#1FBA59` (4.61:1); "Segera Hadir" pill is now dark-on-light instead of muted-on-tint.
- Build (5): `npm install` then `npm run build` passes, 4 pages built. Click-through verified by code inspection against the served build (`astro preview` on :4322): all 10 wa.me links use the correct number, every `/#anchor` (beranda, produk, kandungan, pengiriman, tentang) has a matching id, no dead `#`/empty hrefs, viewport/skip-link/menu-toggle present, map iframe, cert links, blog article links, and both 404 CTAs resolve. Browser-tab automation was unavailable (eval JS worker timed out), so no live click log; full Playwright run also not run (no browsers installed).
- Icons (7): trust-bar price item uses `store`; nutrition cards use distinct `shield`/`nutrition`/`check`. `check` remains the verification glyph on hero list and product badges. Outline set kept as deliberate brand voice.
- Cards (9): nutrition featured card inverted to deep-green highlight; order steps rebuilt as numbered timeline rows instead of a third card grid; products stay commerce cards.
- Badges (10): `Pilihan Keluarga`/`Paling Hemat` kept as real merchandising status, savings computed from `prices.yaml`.
- Motion (11): `.reveal` guides scroll order, card lift signals tappability, hero entrance sets first-view hierarchy, badge pop marks the savings callout; `prefers-reduced-motion` disables them.
- Arrows (12): arrows stay only on forward-movement CTAs (hero `Lihat Pilihan Kemasan`, blog `Lihat Semua`, 404 `Kembali ke Beranda` corrected to arrow-free since it goes back, delivery external-links keep theirs); article "Baca selengkapnya" is now arrow-free text link.
- Glass/shadow/glow (13): `backdrop-blur` limited to sticky header for legibility; shadows stay selective elevation tokens.
- Background/illustrations (14): unchanged, real photos and cert logos.
- Layout (15): delivery kept as asymmetric info-plus-map; producer editorial-plus-image; order steps now timeline.
- Buttons (16): full-pill kept as brand button voice. CTAs/copy/palette/identity (17, 18): unchanged, product-specific CTAs, no buzzwords, 3-plus-1 palette, light-only theme.

## Decision reasons (R-31)

- Flat color over gradients/glows: hierarchy comes from deep-green blocks and yolk accents (R-01).
- Outline icon set: matches rounded brand voice; distinct nutrition icons encode protection/record/verified (R-04).
- Nunito Sans ExtraBold uppercase eyebrows: the section-voice motif (R-06).
- Nutrition featured inversion: value hierarchy, Omega-3 is the headline number (R-14).
- Steps as timeline: process content, not features (R-14).
- Badges: real computed merchandising status (R-09).
- Motion per pattern as above (R-19).
- Arrows only for forward movement (R-08).
- Pill buttons: friendly food-brand voice, consistent across all CTAs (R-11).

## Delivery Gate

- Block 1 Hard Gate: PASS. Single `tentang` anchor verified in built HTML; empty states present; WA contrast 5.94:1 (hover 4.61:1); coming-soon pill dark-on-light; no em dash/stats/testimonials/FAQ added; all anchors resolve; keyboard/menu/skip-link code paths intact; no CSS patching; no theme toggle; build clean.
- Block 2 Purpose-Gate: PASS with written reasons above.
- Block 3 Liveliness: dials ENERGY 1 / RHYTHM 2 / MOTION 1 (calm local-food page, varied section compositions, restrained motion); focal point per screen kept; accent is yolk used sparingly; motif is eyebrow-plus-rounded-cards.
- Block 4 Craftsmanship: PASS. No dead controls (WhatsAppButton requires href; Button without href renders a real `<button>`); sections serve content; palette, CTAs, and identity specific.
- Caveats: live browser click-through not recorded (tooling unavailable); Playwright suite not run (no browsers); E2E header href expectations pre-date the `/#` href format and need owner review.
